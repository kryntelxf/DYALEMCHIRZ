apiVersion: v1
kind: Service
metadata:
  name: {{ include "dyalemchirz.fullname" . }}
  labels:
    {{- include "dyalemchirz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "dyalemchirz.selectorLabels" . | nindent 4 }}
